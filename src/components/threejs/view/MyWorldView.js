import * as THREE from three;

import View from './View';

export default class MyWorldView extends View {
  constructor(renderer, scene, camera, cameraControlsClass) {
    super(renderer, scene, camera, cameraControlsClass);
    
  }
}
